fn main(){
    println!("hello world");
}
//fn means that its a function notation
// main is the obviously main function
// println! is basically a macros here because of !() this
// and it prints the string enclosed in double quotes
// macros basically used in metaprogramming which means code that
// writes code, compiler expands them to the source code which gets compiled with the rest of the program
// and basically semicolon means end of statement